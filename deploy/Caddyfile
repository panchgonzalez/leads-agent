# Replace "your-domain.example" with your real hostname.
# The domain must have a DNS A record pointing to your server's static IP.
your-domain.example {
  encode gzip

  # Proxy all requests to the leads-agent API container
  reverse_proxy primary:8000
}

